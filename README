See http://www.mongodb.org/display/DOCS/PHP+Language+Center for detailed installation instructions.

If you absolutely must, you can install it without reading the wiki:

   $ sudo scons install

Now go read the wiki, because you need to configure php.ini before it will actually work.
