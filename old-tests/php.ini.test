;; These are the options that need to be present in your php.ini file for the
;; driver's tests to pass.

extension=mongo.so
error_reporting = E_STRICT | E_ALL

;; If you are using PHP 5.3, the timezone must also be set.  Change to whatever
;; your local timezone is.
date.timezone = America/New_York

